---
import Layout from '../layouts/Layout.astro'
import Header from '../components/layout/Header.astro'
import Footer from '../components/layout/Footer.astro'

export const prerender = true

const heroTech = [
  'AWS · EKS',
  'Terraform',
  'Kubernetes',
  'ArgoCD',
  'GitOps',
  'Prometheus',
  'Ansible',
  'Python',
]

const stats = [
  ['4+', 'Years in production', 'Real environments, not sandbox'],
  ['3', 'Cloud providers', 'AWS · Azure · GCP'],
  ['100%', 'Infrastructure as Code', 'Zero manual changes'],
  ['0', 'Manual deploys', 'GitOps with ArgoCD'],
]

const projects = [
  {
    name: 'aws-terraform-devops',
    desc: 'Complete AWS infrastructure with EKS 1.31, RDS PostgreSQL 15, ArgoCD, Prometheus/Grafana. Production-ready Terraform modules.',
    tags: ['AWS', 'EKS', 'Terraform', 'ArgoCD'],
    status: 'Production',
    href: 'https://github.com/lra-cloud-ops/aws-terraform-devops',
  },
  {
    name: 'gitops-stack',
    desc: 'GitOps pipeline with ArgoCD on AWS EKS. Zero manual kubectl in production. Every change through Git.',
    tags: ['ArgoCD', 'GitOps', 'Kubernetes', 'Helm'],
    status: 'Production',
    href: 'https://github.com/lra-cloud-ops/gitops-stack',
  },
  {
    name: 'k8s-devops-platform',
    desc: 'Kubernetes platform with KinD, ArgoCD, Prometheus, Grafana. Local dev environment that mirrors production.',
    tags: ['Kubernetes', 'KinD', 'Prometheus', 'Grafana'],
    status: 'Reference',
    href: 'https://github.com/lra-cloud-ops/k8s-devops-platform',
  },
  {
    name: 'linux-fleet-manager',
    desc: '100% ShellCheck validated fleet management scripts. Idempotent operations across Linux hosts via SSH.',
    tags: ['Bash', 'SSH', 'Ansible', 'Linux'],
    status: 'Production',
    href: 'https://github.com/lra-cloud-ops/linux-fleet-manager',
  },
]
---

<Layout
  title="Ruben Liquenson — DevOps Engineer & AWS Specialist"
  description="DevOps Engineer with 4+ years building production-grade cloud infrastructure on AWS. Kubernetes, Terraform, GitOps, ArgoCD."
  lang="en"
>
  <Header />

  <!-- HERO -->
  <section
    class="bg-dark on-dark section hero"
    style="min-height: 90vh; display: flex; align-items: center; position: relative; overflow: hidden;"
  >
    <div class="hero-dots"></div>
    <div class="hero-glow"></div>

    <div class="container" style="position: relative; z-index: 1;">
      <div style="max-width: 760px;">
        <div class="status-pill">
          <span class="status-pill-dot"></span>
          Available for work · Open to remote
        </div>

        <h1 style="color: #FFFFFF; margin-bottom: 1.5rem; line-height: 1.1;">
          DevOps Engineer<br />
          <span style="color: #60A5FA;">&amp; AWS Specialist</span>
        </h1>

        <p
          style="font-size: 1.125rem; color: #94A3B8; max-width: 580px; margin-bottom: 2.5rem; line-height: 1.7;"
        >
          I design and operate production-grade cloud infrastructure on AWS.
          Kubernetes, Terraform, GitOps with ArgoCD — built to last, not to
          impress in demos.
        </p>

        <div
          style="display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 3rem;"
        >
          {heroTech.map((tech) => <span class="tech-pill">{tech}</span>)}
        </div>

        <div style="display: flex; gap: 1rem; flex-wrap: wrap;">
          <a href="/projects" class="btn btn-primary btn-lg btn-pill"
            >View projects →</a
          >
          <a href="/about" class="btn btn-outline-white btn-lg btn-pill"
            >About me</a
          >
          <a
            href="https://www.linkedin.com/in/rubenliquenson-490961269"
            target="_blank"
            rel="noopener"
            class="btn btn-ghost btn-lg btn-pill"
            style="border-color: rgba(255,255,255,0.15); color: #94A3B8;"
            >LinkedIn ↗</a
          >
        </div>
      </div>
    </div>
  </section>

  <!-- STATS -->
  <section class="bg-subtle section-sm">
    <div class="container">
      <div class="stats-grid">
        {
          stats.map(([num, label, desc], i) => (
            <div class={`stat-item ${i < 3 ? 'stat-border' : ''}`}>
              <div class="stat-num">{num}</div>
              <div class="stat-label">{label}</div>
              <div class="stat-desc">{desc}</div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ABOUT PREVIEW -->
  <section class="bg-light section">
    <div class="container">
      <div class="about-grid">
        <div>
          <div class="eyebrow">About me</div>
          <h2 style="margin-bottom: 1.5rem;">
            Building infrastructure that engineers trust
          </h2>
          <p style="margin-bottom: 1.5rem;">
            I'm Ruben, a DevOps Engineer based in Las Palmas de Gran Canaria,
            Spain. For the past 4+ years I've been designing and operating
            production infrastructure — automated, monitored, and built to
            survive real traffic, not just demos.
          </p>
          <p style="margin-bottom: 2rem;">
            I specialize in AWS EKS, Terraform, and GitOps with ArgoCD. Every
            environment I build is reproducible, automated, and monitored from
            day one.
          </p>
          <a href="/about" class="btn btn-secondary btn-pill">Read full bio →</a
          >
        </div>

        <div class="code-block fade-up terminal-preview">
          <div class="code-block-header">
            <span class="code-dot code-dot-red"></span>
            <span class="code-dot code-dot-yellow"></span>
            <span class="code-dot code-dot-green"></span>
            <span
              style="margin-left: 0.5rem; font-size: 0.75rem; color: #8B949E; font-family: var(--font-mono);"
              >ruben@aws-production ~ main</span
            >
          </div>
          <div class="code-block-content">
            <div>
              <span class="code-comment"># Deploy to production EKS</span>
            </div>
            <div>
              <span class="code-command">$ terraform apply -auto-approve</span>
            </div>
            <div style="color: #8B949E;">
              Plan: 14 to add, 0 to change, 0 to destroy.
            </div>
            <div><span class="code-string">✓ aws_vpc.main</span></div>
            <div><span class="code-string">✓ aws_eks_cluster.main</span></div>
            <div><span class="code-string">✓ aws_rds_cluster.db</span></div>
            <div><span class="code-string">✓ module.alb.aws_lb.main</span></div>
            <div style="color: #4ADE80;">
              Apply complete! Resources: 14 added.
            </div>
            <div style="margin-top: 1rem;">
              <span class="code-command">$ kubectl get pods -n production</span>
            </div>
            <div>
              <span class="code-key">webapp-7d4b9f</span>
              <span class="code-string">2/2 Running</span>
            </div>
            <div>
              <span class="code-key">api-6c8d2e</span>
              <span class="code-string">2/2 Running</span>
            </div>
            <div>
              <span class="code-key">worker-9a1f3b</span>
              <span class="code-string">2/2 Running</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- PROJECTS PREVIEW -->
  <section class="bg-subtle section">
    <div class="container">
      <div style="text-align: center; margin-bottom: 4rem;">
        <div class="eyebrow">Open Source</div>
        <h2>Projects on GitHub</h2>
        <p style="max-width: 500px; margin: 1rem auto 0;">
          Real infrastructure projects — production-grade, documented, and open
          source.
        </p>
      </div>

      <div class="projects-grid">
        {
          projects.map((project) => (
            <a
              href={project.href}
              target="_blank"
              rel="noopener noreferrer"
              class="project-card"
            >
              <div style="display: flex; align-items: start; justify-content: space-between; margin-bottom: 0.75rem;">
                <span style="font-family: var(--font-mono); font-size: 0.9375rem; font-weight: 600; color: var(--color-primary);">
                  {project.name}
                </span>
                <span
                  class={`badge ${project.status === 'Production' ? 'badge-green' : 'badge-blue'}`}
                >
                  {project.status}
                </span>
              </div>
              <p style="font-size: 0.875rem; color: var(--text-muted); margin-bottom: 1rem; line-height: 1.6;">
                {project.desc}
              </p>
              <div style="display: flex; flex-wrap: wrap; gap: 0.375rem;">
                {project.tags.map((tag) => (
                  <span class="tech-pill" style="font-size: 0.6875rem;">
                    {tag}
                  </span>
                ))}
              </div>
            </a>
          ))
        }
      </div>

      <div style="text-align: center; margin-top: 3rem;">
        <a href="/projects" class="btn btn-secondary btn-pill"
          >View all projects →</a
        >
      </div>
    </div>
  </section>

  <!-- CTA FINAL -->
  <section class="bg-dark on-dark section" style="text-align: center;">
    <div class="container">
      <div style="max-width: 600px; margin: 0 auto;">
        <div class="eyebrow">Let's work together</div>
        <h2
          style="color: #FFF; margin-bottom: 1rem; font-size: clamp(2rem, 4vw, 3rem);"
        >
          Looking for a DevOps Engineer?
        </h2>
        <p style="color: #94A3B8; margin-bottom: 2.5rem; font-size: 1.125rem;">
          I'm currently open to new opportunities — full-time, contract, or
          freelance. Let's talk about your infrastructure challenges.
        </p>
        <div
          style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;"
        >
          <a href="/contact" class="btn btn-primary btn-lg btn-pill"
            >Get in touch →</a
          >
          <a href="/about" class="btn btn-outline-white btn-lg btn-pill"
            >View my CV</a
          >
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .hero-dots {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(
      circle at 1px 1px,
      rgba(255, 255, 255, 0.05) 1px,
      transparent 0
    );
    background-size: 32px 32px;
    pointer-events: none;
  }
  .hero-glow {
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 80%;
    height: 40%;
    background: radial-gradient(
      ellipse at center,
      rgba(30, 111, 255, 0.12) 0%,
      transparent 70%
    );
    pointer-events: none;
  }
  .status-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(74, 222, 128, 0.1);
    border: 1px solid rgba(74, 222, 128, 0.25);
    color: #4ade80;
    padding: 0.375rem 0.875rem;
    border-radius: 9999px;
    font-size: 0.8125rem;
    font-weight: 600;
    margin-bottom: 2rem;
  }
  .status-pill-dot {
    width: 8px;
    height: 8px;
    background: #4ade80;
    border-radius: 50%;
    animation: pulse 2s infinite;
  }
  @keyframes pulse {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.4;
    }
  }
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
  }
  .stat-item {
    text-align: center;
    padding: 2rem 1rem;
  }
  .stat-border {
    border-right: 1px solid var(--border-light);
  }
  .stat-num {
    font-family: var(--font-display);
    font-size: 2.5rem;
    font-weight: 900;
    color: var(--color-primary);
    line-height: 1;
    letter-spacing: -0.04em;
  }
  .stat-label {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-top: 0.375rem;
  }
  .stat-desc {
    font-size: 0.8125rem;
    color: var(--text-muted);
    margin-top: 0.25rem;
  }
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    align-items: center;
  }
  .project-card {
    display: block;
    background: #fff;
    border: 1px solid var(--border-light);
    border-radius: var(--radius-md);
    padding: 1.5rem;
    text-decoration: none;
    transition: all var(--transition);
  }
  .project-card:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow-md);
    transform: translateY(-2px);
  }
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  @media (max-width: 768px) {
    .terminal-preview {
      display: none;
    }
    .about-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .stat-border {
      border-right: none;
    }
    .projects-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
