name: Sync GitHub data — daily rebuild

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  trigger-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Cloudflare Pages rebuild
        run: |
          curl -s -d "" "${{ secrets.CLOUDFLARE_DEPLOY_HOOK_URL }}"
          echo "Deploy triggered"
